name: Temporary No Echo Guard Candidate
on:
  push:
    branches: [fix/live-spo-toolbar-command-bridge-v3]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 scripts/tmp-remove-local-echo-guard.py
      - name: Verify scope
        run: |
          git diff --check
          mapfile -t changed < <(git diff --name-only | sort)
          expected=(src/components/RichTextEditor.tsx)
          [[ "${changed[*]}" == "${expected[*]}" ]]
      - name: Commit candidate
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/RichTextEditor.tsx
          git commit -m "test: evaluate Live SPO without stale-fragment guard"
          git push origin HEAD:fix/live-spo-toolbar-command-bridge-v3
